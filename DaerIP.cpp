#include <bits/stdc++.h>
using namespace std;

constexpr int SIZE = 1024;
constexpr int LIP = 32, LW = 8;

string IPS[SIZE];
int lt[4];

void LOADIP(int loc) 
{
    for (int i = 0; i < 4; i++) {
        
    }
}

int main()
{
    int T;
    cin >> T;
    while (T--) {
    string s;
    cin >> s;
    regex rs("\\s+");
    return 0;
}